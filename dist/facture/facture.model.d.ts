export declare class Facture {
    id: number;
    dossier: number;
    client: number;
    description: string;
    total: number;
    constructor(id: number, dossier: number, client: number, description: string, total: number);
}
